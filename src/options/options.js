import { CURRENCIES,DEFAULT_CURRENCY } from '../constants/currencies.js';const select=document.querySelector('#targetCurrency'),status=document.querySelector('#status');for(const [code,currency] of Object.entries(CURRENCIES)){const option=document.createElement('option');option.value=code;option.textContent=`${code} — ${currency.name}`;select.appendChild(option);}const {targetCurrency=DEFAULT_CURRENCY}=await chrome.storage.local.get('targetCurrency');select.value=targetCurrency;document.querySelector('#saveButton').addEventListener('click',async()=>{await chrome.storage.local.set({targetCurrency:select.value,currencyPreferenceSet:true});status.textContent=`Saved: ${select.value}`;});
