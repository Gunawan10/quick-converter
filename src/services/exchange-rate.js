const API_BASE_URL='https://api.frankfurter.dev/v2';const CACHE_TTL_MS=60*60*1000;
export async function getRate(from,to,storage=chrome.storage.local,fetchFn=fetch){if(from===to)return{rate:1,date:new Date().toISOString().slice(0,10)};const cacheKey=`rate:${from}:${to}`;const cached=await storage.get(cacheKey);if(cached[cacheKey]&&Date.now()-cached[cacheKey].fetchedAt<CACHE_TTL_MS)return cached[cacheKey].data;const response=await fetchFn(`${API_BASE_URL}/rate/${from}/${to}`);if(!response.ok)throw new Error(`Exchange rate request failed: ${response.status}`);const data=await response.json();const result={rate:data.rate,date:data.date};await storage.set({[cacheKey]:{fetchedAt:Date.now(),data:result}});return result;}
